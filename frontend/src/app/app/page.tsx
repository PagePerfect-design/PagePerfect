import CompileShell from './CompileShell'

export const metadata = {
  title: 'Page Perfect — Editor',
  description: 'Two-panel editor and PDF preview shell.',
}

export default function AppPage() {
  return <CompileShell />
}
