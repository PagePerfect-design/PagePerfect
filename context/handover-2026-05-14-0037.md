# Handover — 2026-05-14 00:37

> Branch: `main` · 0 commits · 1 file modified · ⚠️ **uncommitted changes — massive surface area**

## Mission
Install + scope a comprehensive Claude Code skill/plugin/memory system for PagePerfect (master-build-kit base + cherry-picks from coreyhaines31, BrianRWagner, Anthropic skills, Bencium, Emil Kowalski) and run baseline drift/gap audits.

## ⚠️ Uncommitted work — DO NOT BLOW AWAY
- `CLAUDE.md` modified (910 → 105 lines; content split into `projects/pageperfect/`)
- `.claude/` untracked (62 skill directories + plugin marketplace files)
- `memory/` untracked (7 dept directories + 7 `MEMORY.md` indexes + `PROCESS/VOICE/OUTPUT/TOOLS/CONSTRAINTS/CORRECTIONS.md`)
- `context/` untracked (this handover + 2 audit reports)
- `CLAUDE.md.pre-kit-backup` untracked (preserved original 826-line CLAUDE.md)
**Stage and review before any branch switch or reset.** Recommend committing in 3 logical chunks: (1) memory/+CLAUDE.md split, (2) .claude/skills/ install, (3) context/ audits.

## What landed (all uncommitted)
- ✅ `memory/` tree — PROCESS.md (Standing Council rewritten: Web3 seats #3/#31/#32 → Typography/LaTeX-PDF/Book-publishing), VOICE/OUTPUT/TOOLS/CONSTRAINTS/CORRECTIONS, 7 department dirs with MEMORY.md indexes
- ✅ `.claude/skills/` — 62 skills: kit's 47 (minus 3 AG-only) + emil-design-eng + bencium-typography + 8 marketing cherry-picks + 3 Tier 1 meta-skills (session-orient, stale-detector, gap-audit) + pp-handover + skill-creator
- ✅ `projects/pageperfect/` — PROJECT, ARCHITECTURE, DESIGN, BUSINESS, STATUS, decisions/README; root CLAUDE.md slimmed to load-map
- ✅ Plugins installed via `/plugin install`: skill-creator, superpowers (14 sub-skills), context-mode, claude-mem. Confirmed by `/reload-plugins`: 7 plugins · 9 agents · 8 hooks · 2 MCP servers
- ✅ `memory/design/bold-design-principles.md` — Bencium Tone Options vocab + 6 principles, scoped (Inter Tight kept; photography rejected)
- ✅ Two audit reports: `context/audits/2026-05-14-stale-report.md`, `2026-05-14-gap-report.md`

## Verified
- Plugin counts via `/reload-plugins` output (6→7 after claude-mem)
- Skill discovery via available-skills list (every new SKILL.md surfaced post-reload)
- `gap-audit` confirmed multiple TRANSFORMATION_COUNCIL.md P0s are CLOSED: `uncaughtException`+`unhandledRejection` (`index.js:959,964`), rate limit (`index.js:786`), Stripe HMAC (`routes/stripe.js:21`), Lulu HMAC (`lulu.js:286`), `-raw_tex` disabled (`index.js:429`)
- `stale-detector` produced 8 categorical findings with file:line evidence

## NOT verified — user must do
- ⏳ `.claude/settings.json` write — harness blocked self-modification; suggested JSON is in `CLAUDE.md`. Action: `vim .claude/settings.json` and paste the block
- ⏳ `/context-mode:ctx-doctor` — confirm all `[x]` checks pass
- ⏳ context-mode statusline — manual edit to `~/.claude/settings.json` per CLAUDE.md instructions
- ⏳ ARCHITECTURE.md / PROJECT.md Typst rewrite — **the single highest-leverage fix from these audits**. Backend has migrated to Typst (evidence: `*-typst.test.js`, `entitlements.js`, `result-store.js`, `cluster.js`, `worker.js`, `layout-sanity-checker.js`). Docs still say Pandoc + LuaLaTeX.
- ⏳ AG-residual cleanup across 30 files (`memory/marketing/seo.md` worst at 5 hits)
- ⏳ `claude-mem` precedence rule to `memory/CORRECTIONS.md` (drafted in chat, not written)
- ⏳ 6/7 routes have no tests; 73 frontend components have 0 tests
- ⏳ Only 1 `prefers-reduced-motion` branch for 6 motion locations in `globals.css` — Noor would veto a new animation

## Open threads
- ❓ Did the Typst migration ship behind a flag or fully replace Pandoc? Need to trace `index.js:429` actual usage
- ⚠️ `superpowers` may have its own plan-first conventions that overlap with kit's PROCESS rule #1 — untested under combined load
- ⚠️ `claude-mem` will auto-capture future sessions; precedence-vs-`memory/PROCESS.md` rule still un-codified in CORRECTIONS
- ❌ Tried to write `.claude/settings.json` directly — blocked. Don't retry; user does it manually.
- ❌ Tried to edit `allowed-tools` on existing skill SKILL.md — blocked. Don't retry; body edits fine.

## Decisions
- **Keep CLAUDE.md.pre-kit-backup**: 826-line original preserved during the split. Rejected: delete now; reason for keeping: sanity-check the projects/pageperfect/ split before deleting.
- **Standing Council scoping**: replaced Web3 seats (#3/#31/#32) with Typography/LaTeX-PDF/Book-publishing instead of deleting. Rejected: drop count below 36; reason for keeping count: kit rule says "never remove, only add."
- **emil-design-eng pruning**: removed Spring/Framer-Motion + drag sections; PagePerfect uses plain CSS + `next-view-transitions`, no Motion lib. Rejected: install whole; reason: 600-line cap + 5 KB context per invocation matters.
- **bencium-innovative**: extracted concepts to `memory/design/bold-design-principles.md`; did NOT install full skill. Reason: its Inter ban and "use photography" conflict directly with PagePerfect's intentional system.
- **Skip ui-ux-pro-max / alirezarezvani / ComposioHQ / get-shit-done-cc**: project-bootstrap or overlap-heavy. Cherry-picks only.

## You are here → next step
Rewrite `projects/pageperfect/ARCHITECTURE.md` and `PROJECT.md` Tech Stack section to reflect the **Typst migration**. Start by reading `backend/index.js:420-440` (current Pandoc invocation), `backend/compile-worker.js`, `backend/typst-error-translator.js`, `backend/worker.js`, `backend/cluster.js`, and the four `*-typst.test.js` files. Determine: is Typst the primary engine now, a parallel path, or a flag-gated migration? Then update docs + write `projects/pageperfect/decisions/0001-typst-migration.md`.
